# Token signing secret — change this to a long random string in production
TOKEN_SECRET=change-me-to-a-long-random-secret

# Valid users as JSON array — each entry requires email + phone to match
VALID_USERS=[{"email":"user@example.com","phone":"0900000000"}]
